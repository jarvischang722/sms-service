const strUtils = require('../../utils/str')


const generateVerifyCode = () => {
  // 產生驗證碼
  let verifiCode

  return verifiCode
}

const sendVerifyCode = () => {
  // 寄送驗證碼給玩家
}

const checkVerifyCode = () => {
  // 驗證手機號是否正確
}

module.exports = {
  generateVerifyCode,
  sendVerifyCode,
  checkVerifyCode
}
